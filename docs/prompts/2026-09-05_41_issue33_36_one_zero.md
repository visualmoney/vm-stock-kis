# 2026-09-05 - #33–#36 1.0.0 호환 제거 착수

## 사용자 요청
> #33, #34, #35, #36 착수

## 분석
- `#30` 닫힘. `blocked` 해제. `next-up` 은 33·34·35 (최대 3). `#36` 은 같은 PR.
- `v1.0.0` 태그는 네 건 착지 후 별도.
- 게이트를 소유자 조기 착수 근거로 고친다.

## 계획
1. `#33` 폴백 3종 제거 + 검사 뒤집기
2. `#34` 루트 → types 위임 제거
3. `#35` Development Status Stable
4. `#36` MIGRATION / 안정성 정책 / CHANGELOG
5. 게이트 시한폭탄을 조기 착수 기록으로 교체

## 결과
`#33`–`#36` 코드·문서 반영. 게이트를 폴백 제거 검사로 교체. `v1.0.0` 태그는 안 침.
