# 2026-09-05 - next-up 대기열 재구성

## 사용자 요청
> plan - next-up

## 분석
- `next-up` 이 비어 있다. 직전 3건(#92 · #95 · #103)은 모두 닫혔다.
- `#108` 자식 5건(#103 · #104 · #105 · #106 · #107)도 모두 닫혔다.
- `needs-decision`(#30 · #100)과 `blocked`(#33~#36)는 대기열에 넣지 않는다.

## 계획
1. 열려 있는 이슈 본문·코멘트를 읽고 사용자 피해로 정렬
2. 최대 3건에 `next-up` 부여
3. 일이 끝난 상위 이슈 `#108` 을 닫는다

## 결과
`#111` → `#112` → `#96`. `#108` 닫음. `#94` 는 다음 교체 후보.
