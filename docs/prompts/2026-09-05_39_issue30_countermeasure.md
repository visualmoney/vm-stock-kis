# 2026-09-05 - #30 미충족 대책 실행

## 사용자 요청
> #30 미충족 대책. 시세·연결이 0이면 동작 충족. get_balance / account_lookups 는 필수 연기에서 뺀다.

## 분석
- 구조·제공·겉면은 이미 충족.
- `#33`–`#36` 은 당기지 않는다. `#155` 와 섞지 않는다.
- `#157` 에 skip·mkdir 이 이미 있다. 목록만 맞추고 PR 한다.

## 계획
1. 결정을 `#30` 에 쓴다
2. 필수 연기에서 잔고·계좌를 뺀다
3. `#157` PR (`Closes #157`)
4. `#30` 재검사 후 닫는다. `#33`–`#36` 은 그대로

## 결과
`#30` 에 동작 선을 씀. 필수 연기에서 잔고·계좌를 뺌. `#157` PR. `#33`–`#36` 안 염.
