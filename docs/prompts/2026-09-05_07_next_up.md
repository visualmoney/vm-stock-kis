# 2026-09-05 - next-up 대기열 재구성

## 사용자 요청
> plan - next-up issues? → 승인: 잔여 브랜치 검사 · types.py · #100 결정
> 그리고 파이썬 3.14도 지원이 가능할 것으로 보임.

## 분석
- `next-up` 이 비어 있다. 열린 6건은 `needs-decision`(#30 · #100) 또는 `blocked`(#33~#36).
- #30 계열은 2026-11-27 게이트라 대기열에 넣지 않는다.
- #119 를 오늘 닫았는데 오늘 머지분 5개가 다시 `origin` 에 있다.
- #94 가 남긴 `types.py` `현재(0.0.1)` 은 살아 있는 마크다운 검사 밖이다.
- #100 은 구현이 아니라 A/B/C 결정. 대기열 3번.

## 계획
1. 잔여 브랜치 검사 이슈를 연다
2. `types.py` 낡은 버전 문장 이슈를 연다
3. 셋에 `next-up` 을 단다. #100 의 `needs-decision` 은 유지

## 결과
`#125` → `#126` → `#100`. `#127`(3.14) 은 다음 교체 후보. #30 계열은 그대로.
