# 2026-09-05 - #30 닫는 조건 결정

## 사용자 요청
> #30 닫는 조건은 개선된 software 구조와 업스트림과 동일 수준(범위)의
> 예제 제공 및 동작 입니다. 동일 수준이므로 1.0.0 입니다.

## 분석
- 시점을 날짜로 정한 것이 아니라 1.0.0 의 **뜻**을 정한 것.
- 폴백 제거(`#33`–`#36`)와 2026-11-27 게이트는 당기지 않음.
- `#100` B (현물 밖은 `fetch()`) 와 "업스트림 범위"가 겹치면 본문에 적는다.

## 계획
1. 결정을 `#30` 본문·댓글에 쓴다
2. `needs-decision` 을 뗀다. 이슈는 조건이 충족될 때까지 연다
3. 서브이슈 `blocked` 는 유지

## 결과
`#30` 본문에 닫는 조건을 썼고 `needs-decision` 을 뗐습니다.
`#33`–`#36` 은 그대로 `blocked` 입니다.
