# 2026-09-05 - #111 튜토리얼 노트북 스키마

## 사용자 요청
> #111 착수

## 분석
- `examples/tutorial_basic.ipynb` 셀 5가 폐기된 평면 YAML 을 출력한다.
- 셀 6 은 주석으로만 부정한다. 노트북을 여는 사람은 셀 5를 먼저 실행한다.
- `#95` 가 경로만 고치지 않은 이유: 틀린 스키마를 최신처럼 보이게 만든다.
- 닫힘 조건: `_example_docs()` 에 `.ipynb` 를 넣는 것.

## 계획
1. 셀 4~6 은 스키마를 복제하지 않고 `configs/template_account_profiles.yaml` 을 가리킨다
2. "더 이상 쓰지 않습니다" 주석을 없앤다
3. `_example_docs()` 에 노트북을 넣고, 평면 스키마 회귀를 따로 둔다
4. 결함을 되살려 검사가 실패하는지 확인한다

## 결과
셀 4~6 은 템플릿을 가리키고 `create_client()` 를 부른다. 스키마는
복제하지 않았다. `_example_docs()` 에 `.ipynb` 를 넣었고, 평면 스키마
회귀를 되살려 실패하는 것을 확인했다. 커밋은 아직이다.
