# 2026-09-05 - #30 착수

## 사용자 요청
> #30 착수

## 분석
- `#30` 은 결정 이슈. 닫는 조건은 "시점을 정했다".
- 게이트는 `tests/unit/test_release_gate.py` 의 2026-11-27.
- `#33`–`#36` 은 시점이 정해지기 전에 열지 않음.

## 계획
1. 본문·댓글·게이트·사용 신호를 읽는다
2. 선택지를 이슈에 적고, 정하기 전에는 `needs-decision` 을 떼지 않는다

## 결과
선택지 3개를 `#30` 에 적었습니다. 폴백 제거는 열지 않았습니다.
고르기 전에는 `needs-decision` 유지.
