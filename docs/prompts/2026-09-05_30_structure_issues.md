# 2026-09-05 - 1.0.0 구조 한 줄과 남은 이슈

## 사용자 요청
계획: 구조 한 줄을 살아 있는 문서에 쓰고, 문서 이슈와 `#136` 표 갱신
이슈를 `next-up` 에 단다. `#33`–`#36` 은 열지 않음.

## 분석
- `next-up` 비어 있음. `#30` 이 부모.
- 한 줄: 허브-스포크만 모듈 구조. 파사드는 `stock`/`account`. `SimpleKIS` 는 선택. `fetch()` 는 EXTENDING_API.

## 계획
1. 이슈 2건 + `#30` 댓글
2. ARCHITECTURE · INDEX · USER_GUIDE · SIMPLEKIS_GUIDE
3. 현재 `examples/` 로 표를 맞춰 2번 이슈 본문에 남긴다

## 결과
이슈 `#149` · `#150` (`next-up`). 한 줄은 살아 있는 문서에 씀.
