# 2026-09-05 - #126 types.py 낡은 현재 버전

## 사용자 요청
> #126 착수, 현재 태그된 버전은 0.1.0 인 상황입니다.

## 분석
- 고칠 줄은 `현재(0.0.1)` 하나. 표의 `0.0.x | 활성` 은 호환 경로 서술이라 유지.
- `현재(0.1.0)` 으로 바꾸면 #100 뒤 0.2.0 태그에서 또 낡는다. 숫자를 박지 않는다.

## 계획
1. 그 한 줄을 호환 경로가 동작 중이라는 말로 바꾼다
2. `현재(0.0.1)` 이 다시 생기면 실패하는 검사를 넣는다
3. 표 행이 남아 있는지 본다

## 결과
`현재(0.0.1)` 을 `지금` 으로 바꿈. `현재(0.1.0)` 도 검사에 넣음 — #100 뒤 0.2.0 예정.
