# 2026-09-05 - #136 PyKis 목차 공백 표

## 사용자 요청
> #136 착수

## 분석
- 닫는 조건은 표이지 빈 칸을 채우는 것이 아니다.
- 예제는 `examples/` 파일. USER_GUIDE 코드 조각은 문서다.
- `sellable()` 은 가이드에만 있고 코드에는 `orderable()` 이다.

## 계획
1. 업스트림 2.x 튜토리얼 절로 표
2. `examples/README.md` 바닥의 버전·날짜를 지운다
3. 후속 이슈는 열지 않는다

## 결과
- `#136` 본문에 업스트림 2.x 튜토리얼 목차 공백 표를 적었습니다.
- `examples/README.md` 바닥의 `버전: 1.0.0` / `2025-12-19` 를 지웠습니다.
- `tests/unit/test_examples_readme_no_version_pin.py` 가 그 바닥을 다시 박으면 실패합니다.
- 후속 이슈는 열지 않았습니다. 커밋·PR 은 요청을 기다립니다.
