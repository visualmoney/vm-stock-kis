# 2026-09-05 - 서버 토큰 동기화 스크립트

## 사용자 요청
> /home/claude/github.com/vm-quant-lab/scripts/sync_tokens_from_server.sh
> 같은 파일이 필요함. 이와 같은 역할을 하는 쉘스크립트를 scripts 폴더에
> 만들어 주고, 개인 서버 정보가 있으므로 gitignore 항목을 만들어주고,
> sample or example 스크립트는 버전 관리에 등록함 (이슈로 만든 후 작업하기,
> 목적: 서버의 토큰을 복사하여 중복 생성을 방지하고자 함.) ssh stock-bot
> 으로 접근 가능합니다. next-up label 필요성: token이 expired 된 경우
> 업데이트 할 필요가 있음. 커밋하지 않는 세션 문서 머지하기 승인.

## 분석
- 참고 스크립트는 서버→로컬 단방향 복사, 값 미출력, 0600, cron 비권장.
- 개인 정보는 원격 토큰 디렉터리와 파일명 대응이다. SSH 별칭 `stock-bot` 은
  공개 기본값으로 둔다.
- 정본 예제는 추적하고, 채워진 스크립트는 gitignore 한다.
- 세션 문서 01·02 는 같은 PR 에 실어 머지한다.

## 계획
1. 이슈를 열고 `next-up` 을 붙인다
2. example 스크립트 + gitignore + 검사
3. 로컬용 채워진 스크립트는 추적하지 않는다
4. 커밋·PR·머지

## 결과
이슈 #121. 예제 스크립트와 gitignore·검사를 넣었다. 채워진 로컬
스크립트는 추적하지 않는다. 세션 문서 01·02 와 함께 머지한다.
