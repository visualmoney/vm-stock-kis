# 2026-09-05 - #132 0.2.0 릴리스

## 사용자 요청
> 0.2.0 릴리스
> Implement the plan as specified. CHANGELOG 후 v0.2.0rc1 → TestPyPI,
> 이어서 v0.2.0 → PyPI.

## 분석
- 버전은 git 태그만 출처. `pyproject.toml` 에 쓰지 않는다.
- `#100` 전량 이관은 이 버전에 없다.
- `#30` 계열은 2026-11-27 게이트라 손대지 않는다.
- 살아 있는 문서의 `현재` + `0.1.x` 는 태그 뒤 CI 가 잡는다. `0.x` 로 푼다.

## 계획
1. 이슈 `#132` · `next-up` · 이 프롬프트
2. CHANGELOG 를 `[0.2.0]` 으로 옮기고 정책/아키텍처의 현재 계열을 `0.x` 로
3. 로컬 빌드 검증
4. `v0.2.0rc1` → TestPyPI, `v0.2.0` → PyPI

## 결과
(작업 후 작성)
