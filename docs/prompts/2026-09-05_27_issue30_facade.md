# 2026-09-05 - #30 구조: 얇은 파사드와 확장

## 사용자 요청
> 허브-스포크 구조 앏언 퍼사드 구조로 사용자가 알아야 할 범위가 줄고,
> EXTENDING_API 와 같은 확장 용이성

## 분석
- "앏언 퍼사드" = 얇은 파사드.
- 사용자 표면은 `create_client` · `kis.stock` / `kis.account` · `SimpleKIS`.
- 확장은 `fetch()` / EXTENDING_API. `#100` B 와 같음.
- `#33`–`#36` 은 당기지 않음.

## 계획
`#30` 닫는 조건의 "개선된 구조"에 이 한 줄을 넣는다.

## 결과
`#30` 본문·댓글에 얇은 파사드와 EXTENDING_API 를 넣었습니다.
