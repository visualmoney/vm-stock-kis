# 2026-09-05 - #139 → #140 → #141 착수

## 사용자 요청
> #139 → #140 → #141 착수

## 분석
- `#139` 가 먼저다. 예제가 틀린 이름을 복사하면 안 된다.
- `#140` 은 목차마다 실제 이름을 부르면 된다. IOC/FOK 전 목록은 아니다.
- `#141` 은 README `virtual_secret.json` 과 미정의 `hynix`/`account`.
- `#30` 은 건드리지 않는다. 우리 위키에 Home 을 복사하지 않는다.

## 계획
1. USER_GUIDE · FAQ 호출을 Tutorial 이름으로
2. `examples/01_basic/` 에 빈 칸 예제
3. README 파일명·스니펫
4. 회귀 검사. 커밋은 요청을 기다린다

## 결과
세 이슈의 완료 기준을 로컬에서 채웠습니다. 커밋·PR 은 요청을 기다립니다.
