# 2026-09-05 - 초·중·고 커리큘럼 접기

## 사용자 요청
> keep going

## 분석
- `#158` 머지. `#30` 닫힘. `v1.0.0` 은 안 찍음.
- 다음 `next-up` 은 `#155`. 02/03 을 지우고 `01_basic` 만 남긴다.
- 펼치거나 합치거나 archive 하지 않는다. `#33`–`#36` 안 염.

## 계획
1. `#157` leftover `next-up` 을 뗀다
2. 02/03 삭제, 살아 있는 문서 수정, 폴더 회귀 검사
3. PR `Closes #155`

## 결과
02/03 삭제. 살아 있는 문서에서 경로를 뺌. 레이아웃 검사 추가. `#33`–`#36` 안 염. `v1.0.0` 안 찍음.
