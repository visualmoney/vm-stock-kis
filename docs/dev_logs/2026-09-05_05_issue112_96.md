# 2026-09-05 - #112 · #96 개발 일지

## 작업 내용

PR #118 을 머지한 뒤 next-up `#112` · `#96` 을 같이 닫습니다.

#112: `#115` 이후 다시 세고 세 종류로 나눴습니다. 복사해 쓰는 호출만
`configs/account_profiles.yaml` 로 바꿨습니다. 마이그레이션·안정성 정책의
`PyKis("config.yaml")` / `VmKis("config.yaml")` 짝은 그대로 둡니다.

#96: `USER_GUIDE` 시세 조회 절 앞에 요지를 넣고, README 에 한 줄,
영문 한 장에 한 문장을 넣었습니다. 약관 전문은 없습니다.

## 밟은 함정

`docs/` 이름 검사를 예제와 같이 전면 확장하면 마이그레이션 문서가
빨개집니다. 막는 것은 `create_client("config.yaml")` 과
`save_config_interactive("config.yaml")` 뿐입니다.

CONFIG_SCHEMA 의 인용은 `src/` 에 없는 `config.yaml 에 version` 이었습니다.
실제 R1 은 읽은 경로를 앞에 붙입니다.

검색으로 나온 PDF 는 *제휴 이용기관* 약관이라 제5조 ③ 내용이 다릅니다.
원문 링크는 포털만 가리키고, 그 PDF 는 쓰지 않았습니다.

## 변경 파일

- `docs/SIMPLEKIS_GUIDE.md`, `docs/MIGRATION_GUIDE.md` (현재 도구 절만),
  `docs/NEWSLETTER_TEMPLATE.md`, `docs/FAQ.md`,
  `docs/guidelines/REGIONAL_GUIDES.md`, `docs/guidelines/CONFIG_SCHEMA.md`,
  `SECURITY.md`, `SECURITY.en.md`
- `docs/user/USER_GUIDE.md`, `README.md`, `docs/user/en/README.md`
- `tests/unit/test_living_docs_config_path.py`

## 테스트 결과

관련 단위 검사는 통과했습니다. SIMPLEKIS 에
`create_client("config.yaml")` 을 되넣으면 실패합니다.

## 다음 할 일

`next-up` 교체 후보는 `#94` 입니다.
