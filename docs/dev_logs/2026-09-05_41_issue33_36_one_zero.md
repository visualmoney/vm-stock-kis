# 2026-09-05 - #33–#36 1.0.0 호환 제거 일지

## 작업 내용

소유자 착수. `#30` 닫힌 뒤 `blocked` 해제.
`v1.0.0` 태그는 치지 않음 — 코드·문서만.

| 이슈 | 내용 |
|---|---|
| `#33` | `PyKis` · `~/.pykis` · `PYKIS_*` 제거 |
| `#34` | 루트 → `types` `__getattr__` 위임 제거 |
| `#35` | Development Status → Stable |
| `#36` | MIGRATION · 안정성 정책 · CHANGELOG Breaking |

게이트 시한폭탄을 폴백 제거 검사로 바꿨습니다.
루트 위임이 사라지자 README·USER_GUIDE 의 긴 타입 import 가 검사에 잡혔습니다.
공개 별칭(`Quote` 등)과 `vmkis.types` 로 고쳤습니다.

## 결함 재현

- `vmkis.PyKis` → `AttributeError`
- `from vmkis import KisObjectProtocol` → `ImportError`
- `PYKIS_*` 만 있으면 `_env` 가 `None`

## 다음

PR 머지 후 `v1.0.0` 태그는 별도 지시.
