# 2026-09-05 - next-up 재구성 개발 일지

## 작업 내용

열린 구현 이슈가 없어 새 이슈 2건을 연 뒤 대기열을 채웠다.

| 순서 | 이슈 | 왜 |
|---|---|---|
| 1 | #125 | #119 를 오늘 닫았는데 같은 잔여가 다시 보였다. 한 번 지우는 이슈는 반복된다 |
| 2 | #126 | #94 가 마크다운만 봤고 `types.py` `현재(0.0.1)` 은 검사 밖 |
| 3 | #100 | codegen A/B/C. `needs-decision` 유지. 닫는 조건은 결정 |

#30 과 자식 4건은 2026-11-27 게이트라 넣지 않았다.

추가로 #127 을 열었다. 3.14 가 될 것 같다는 확인이다. 대기열은 3칸이라
`next-up` 은 달지 않았다. 다음 교체 후보다.

## 밟은 함정

이슈를 열 때 `git ls-remote --heads origin` 에는 `main` 과
`draft/config-schema-v2` 만 있었다. 아침에 보이던 머지분 5개는 원격에서
이미 사라졌거나, fetch --prune 없는 `git branch -r` 잔여였다. 그래서
#125 완료 기준에서 "지금 5개를 지운다"를 빼고 검사만 남겼다.

`git branch -r` 로 잔여를 세면 #119 를 무한히 다시 열게 된다.

## 변경 파일

- `docs/prompts/2026-09-05_07_next_up.md`
- `docs/dev_logs/2026-09-05_07_next_up.md`

## 테스트 결과

해당 없음.

## 다음 할 일

대기열 1번 `#125` 부터.
