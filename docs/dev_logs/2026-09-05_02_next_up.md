# 2026-09-05 - next-up 재구성 개발 일지

## 작업 내용

직전 대기열(#92 · #95 · #103)과 `#108` 자식 5건이 모두 닫힌 뒤 큐가
비어 있었다. 사용자 피해로 다시 짰다.

| 순서 | 이슈 | 왜 |
|---|---|---|
| 1 | #111 | 튜토리얼 셀 5가 거부되는 스키마를 만든다 |
| 2 | #112 | 살아 있는 문서가 없는 `config.yaml` 을 가리킨다. `#115` 이후 다시 세야 한다 |
| 3 | #96 | PyPI 에 나갔는데 시세 재배포 금지가 사용자 문서에 없다 |

`#108` 은 자식이 끝나 닫았다. `#94` 는 다음 교체 후보다.

## 밟은 함정

`#96` 완료 기준의 *"en 에도 반영"* 은 `#104` 이전 문장이다. 영문은 한 장만
남았다. 이슈에 코멘트로 좁혀 두었다.

`#112` 제목의 "32곳"은 `#115` 가 `VIDEO_SCRIPT` 를 archive 로 보낸 뒤로는
틀린 숫자다. 착수 전에 `git grep` 으로 다시 세라는 코멘트가 이미 있다.

## 변경 파일

- `docs/prompts/2026-09-05_02_next_up.md`
- `docs/dev_logs/2026-09-05_02_next_up.md`

## 테스트 결과

해당 없음.

## 다음 할 일

대기열 1번 `#111` 부터.
